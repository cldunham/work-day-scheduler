$(document).ready(function () {
})

// current day at the top of the calendar
$("#currentDay").text(moment().format('dddd MMMM Do YYYY'));

//timeblocks and color changes
function colorChange() {
    
}
